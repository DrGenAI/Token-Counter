# Third party notices

## gpt-tokenizer

`src/tokenizer.bundle.js` is a build of [gpt-tokenizer](https://github.com/niieani/gpt-tokenizer)
by Bazyli Brzoska, containing the `o200k_base` encoding only. Licensed MIT; the
full licence text is in `vendor-gpt-tokenizer-LICENSE.txt`.

Rebuild it with `tools/build.sh`.

## Prior art

The interface follows [claude-counter](https://github.com/she-llac/claude-counter)
by she-llac (MIT), which does the same job for claude.ai. No code is shared.
